# only for superhost
AddHandler application/x-httpd-php5 .php

# Rewrite rules for Musca
RewriteEngine On
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^(.*)$ index.php [L]
